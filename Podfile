# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GetTheDot' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GetTheDot
pod 'Google-Mobile-Ads-SDK'
pod ‘SwiftySound’
pod ‘SwiftyStoreKit’
pod 'Firebase/Core'

  target 'GetTheDotTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'Google-Mobile-Ads-SDK'
    pod ‘SwiftySound’
    pod ‘SwiftyStoreKit’
  end

  target 'GetTheDotUITests' do
    inherit! :search_paths
    # Pods for testing

  end

end
